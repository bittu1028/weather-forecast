import React from 'react';
import { StyledLoaderCircular, StyledLoaderContainer } from './StyledLoader';

const Loader = () => (
  <StyledLoaderContainer>
    <StyledLoaderCircular />
  </StyledLoaderContainer>
);

export default Loader;